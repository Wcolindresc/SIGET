import { useEffect,useState } from 'react'; import { api } from '../api'; import { Card,Page } from '../components/UI'; export function Reportes(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api.get('/reportes/equipos-por-estado').then(r=>setRows(r.data))},[]);return <Page title="Reportes"><Card><h3>Equipos por estado</h3><table><thead><tr><th>Estado</th><th>Cantidad</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.estado}><td>{r.estado}</td><td>{r._count._all}</td></tr>)}</tbody></table></Card></Page>}