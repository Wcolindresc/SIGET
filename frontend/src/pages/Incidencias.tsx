import { FormEvent,useEffect,useState } from 'react'; import { api } from '../api'; import { Badge,Card,Page } from '../components/UI'; export function Incidencias(){const [rows,setRows]=useState<any[]>([]),[equipos,setEquipos]=useState<any[]>([]); const load=()=>api.get('/incidencias').then(r=>setRows(r.data)); useEffect(()=>{load();api.get('/equipos').then(r=>setEquipos(r.data))},[]); async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);await api.post('/incidencias',Object.fromEntries(fd.entries()));e.currentTarget.reset();load()} return <Page title="Incidencias"><Card><form className="gridForm" onSubmit={save}><select name="equipoId" required><option value="">Equipo</option>{equipos.map(x=><option key={x.id} value={x.id}>{x.codigoInventario} - {x.nombre}</option>)}</select><input name="titulo" placeholder="Título" required/><input name="descripcion" placeholder="Descripción" required/><select name="severidad"><option>BAJA</option><option>MEDIA</option><option>ALTA</option><option>CRITICA</option></select><button>Reportar</button></form></Card><Card><table><thead><tr><th>Equipo</th><th>Título</th><th>Severidad</th><th>Estado</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.equipo?.nombre}</td><td>{r.titulo}</td><td><Badge>{r.severidad}</Badge></td><td>{r.estado}</td></tr>)}</tbody></table></Card></Page>}