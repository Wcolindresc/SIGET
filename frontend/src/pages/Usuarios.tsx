import { FormEvent,useEffect,useState } from 'react'; import { api } from '../api'; import { getUser } from '../auth'; import { Card,Page } from '../components/UI'; export function Usuarios(){const u=getUser()!;const [rows,setRows]=useState<any[]>([]); const load=()=>api.get('/usuarios').then(r=>setRows(r.data)); useEffect(()=>{if(u.rol==='ADMINISTRADOR')load()},[]); if(u.rol!=='ADMINISTRADOR')return <Page title="Usuarios"><Card>Módulo exclusivo del administrador.</Card></Page>; async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);await api.post('/usuarios',Object.fromEntries(fd.entries()));e.currentTarget.reset();load()} return <Page title="Usuarios"><Card><form className="gridForm" onSubmit={save}><input name="nombre" placeholder="Nombre" required/><input name="apellido" placeholder="Apellido" required/><input name="correo" type="email" placeholder="Correo" required/><input name="password" type="password" placeholder="Contraseña" minLength={8} required/><select name="rol">{['ADMINISTRADOR','ENCARGADO_INVENTARIO','USUARIO','TECNICO'].map(x=><option key={x}>{x}</option>)}</select><button>Crear usuario</button></form></Card><Card><table><thead><tr><th>Nombre</th><th>Correo</th><th>Rol</th><th>Activo</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.nombre} {r.apellido}</td><td>{r.correo}</td><td>{r.rol}</td><td>{r.activo?'Sí':'No'}</td></tr>)}</tbody></table></Card></Page>}