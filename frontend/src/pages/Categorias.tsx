import { FormEvent,useEffect,useState } from 'react'; import { api } from '../api'; import { Card,Page } from '../components/UI'; export function Categorias(){const [rows,setRows]=useState<any[]>([]); const load=()=>api.get('/categorias').then(r=>setRows(r.data)); useEffect(()=>{load()},[]); async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);await api.post('/categorias',Object.fromEntries(fd.entries()));e.currentTarget.reset();load()} return <Page title="Categorías"><Card><form className="inlineForm" onSubmit={save}><input name="nombre" placeholder="Nueva categoría" required/><input name="descripcion" placeholder="Descripción"/><button>Agregar</button></form></Card><Card><table><thead><tr><th>Nombre</th><th>Descripción</th><th>Estado</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.nombre}</td><td>{r.descripcion}</td><td>{r.activo?'ACTIVA':'INACTIVA'}</td></tr>)}</tbody></table></Card></Page>}