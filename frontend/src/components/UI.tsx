import React from 'react'; export const Page=({title,actions,children}:{title:string;actions?:React.ReactNode;children:React.ReactNode})=><><div className="pageTitle"><h1>{title}</h1><div>{actions}</div></div>{children}</>; export const Card=({children}:{children:React.ReactNode})=><div className="card">{children}</div>; export const Badge=({children}:{children:React.ReactNode})=><span className="badge">{children}</span>; export const ErrorBox=({text}:{text:string})=>text?<div className="error">{text}</div>:null;