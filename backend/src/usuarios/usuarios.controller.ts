import { Body,Controller,Get,Param,Patch,Post,UseGuards } from '@nestjs/common'; import { Rol } from '@prisma/client'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { CreateUsuarioDto } from './dto/create-usuario.dto'; import { UpdateUsuarioDto } from './dto/update-usuario.dto'; import { UsuariosService } from './usuarios.service'; @UseGuards(JwtAuthGuard,RolesGuard) @Roles(Rol.ADMINISTRADOR) @Controller('usuarios') export class UsuariosController{constructor(private s:UsuariosService){} @Get() list(){return this.s.list()} @Get(':id') one(@Param('id') id:string){return this.s.one(id)} @Post() create(@Body() d:CreateUsuarioDto){return this.s.create(d)} @Patch(':id') update(@Param('id') id:string,@Body() d:UpdateUsuarioDto){return this.s.update(id,d)} @Patch(':id/activar') activar(@Param('id') id:string){return this.s.update(id,{activo:true})} @Patch(':id/desactivar') desactivar(@Param('id') id:string){return this.s.update(id,{activo:false})}}