import { ConflictException,Injectable,NotFoundException } from '@nestjs/common'; import * as bcrypt from 'bcrypt'; import { PrismaService } from '../prisma/prisma.service'; import { CreateUsuarioDto } from './dto/create-usuario.dto'; import { UpdateUsuarioDto } from './dto/update-usuario.dto'; @Injectable() export class UsuariosService{constructor(private p:PrismaService){} async list(){return this.p.usuario.findMany({select:{id:true,nombre:true,apellido:true,correo:true,rol:true,activo:true,fechaCreacion:true,fechaActualizacion:true},orderBy:{fechaCreacion:'desc'}})} async one(id:string){const u=await this.p.usuario.findUnique({where:{id},select:{id:true,nombre:true,apellido:true,correo:true,rol:true,activo:true,fechaCreacion:true,fechaActualizacion:true}}); if(!u)throw new NotFoundException('Usuario no encontrado'); return u;} async create(d:CreateUsuarioDto){try{return await this.p.usuario.create({data:{nombre:d.nombre,apellido:d.apellido,correo:d.correo,passwordHash:await bcrypt.hash(d.password,12),rol:d.rol,activo:d.activo??true},select:{id:true,nombre:true,apellido:true,correo:true,rol:true,activo:true}})}catch{throw new ConflictException('Correo ya registrado')}} async update(id:string,d:UpdateUsuarioDto){await this.one(id); return this.p.usuario.update({where:{id},data:{nombre:d.nombre,apellido:d.apellido,correo:d.correo,rol:d.rol,activo:d.activo,...(d.password?{passwordHash:await bcrypt.hash(d.password,12)}:{})},select:{id:true,nombre:true,apellido:true,correo:true,rol:true,activo:true}})} }