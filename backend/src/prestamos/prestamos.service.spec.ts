import { BadRequestException } from '@nestjs/common'; import { PrestamosService } from './prestamos.service'; describe('PrestamosService',()=>{it('impide fecha límite inválida',async()=>{const s=new PrestamosService({} as any); await expect(s.create({usuarioId:'u',equipoId:'e',fechaPrestamo:'2026-10-02',fechaLimite:'2026-10-01'},'a')).rejects.toBeInstanceOf(BadRequestException);});});