import { Body,Controller,Get,Param,Post,Put,UseGuards } from '@nestjs/common'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { CurrentUser } from '../common/decorators/current-user.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { IncidenciaDto,IncidenciaUpdateDto } from './dto/incidencia.dto'; import { IncidenciasService } from './incidencias.service'; @UseGuards(JwtAuthGuard,RolesGuard) @Controller('incidencias') export class IncidenciasController{constructor(private s:IncidenciasService){} @Post() create(@Body() d:IncidenciaDto,@CurrentUser() u:{id:string}){return this.s.create(d,u.id)} @Get() list(){return this.s.list()} @Get(':id') one(@Param('id') id:string){return this.s.one(id)} @Put(':id') update(@Param('id') id:string,@Body() d:IncidenciaUpdateDto){return this.s.update(id,d)}}