import { Body,Controller,Get,Param,Post,Put,UseGuards } from '@nestjs/common'; import { Rol } from '@prisma/client'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { CurrentUser } from '../common/decorators/current-user.decorator'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { ReservaDto,RespuestaReservaDto } from './dto/reserva.dto'; import { ReservasService } from './reservas.service'; @UseGuards(JwtAuthGuard,RolesGuard) @Controller('reservas') export class ReservasController{constructor(private s:ReservasService){} @Post() create(@CurrentUser() u:{id:string},@Body() d:ReservaDto){return this.s.create(u.id,d)} @Roles(Rol.ADMINISTRADOR,Rol.ENCARGADO_INVENTARIO) @Get() list(){return this.s.list()} @Get('mias') mine(@CurrentUser() u:{id:string}){return this.s.mine(u.id)} @Get(':id') one(@Param('id') id:string){return this.s.one(id)} @Roles(Rol.ADMINISTRADOR,Rol.ENCARGADO_INVENTARIO) @Put(':id/aprobar') approve(@Param('id') id:string,@CurrentUser() u:{id:string},@Body() d:RespuestaReservaDto){return this.s.approve(id,u.id,d.observacionRespuesta)} @Roles(Rol.ADMINISTRADOR,Rol.ENCARGADO_INVENTARIO) @Put(':id/rechazar') reject(@Param('id') id:string,@CurrentUser() u:{id:string},@Body() d:RespuestaReservaDto){return this.s.reject(id,u.id,d.observacionRespuesta)} @Put(':id/cancelar') cancel(@Param('id') id:string,@CurrentUser() u:{id:string;rol:Rol}){return this.s.cancel(id,u)}}