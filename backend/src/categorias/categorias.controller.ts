import { Body,Controller,Get,Param,Patch,Post,UseGuards } from '@nestjs/common'; import { Rol } from '@prisma/client'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { CategoriaDto,CategoriaUpdateDto } from './dto/categoria.dto'; import { CategoriasService } from './categorias.service'; @UseGuards(JwtAuthGuard,RolesGuard) @Controller('categorias') export class CategoriasController{constructor(private s:CategoriasService){} @Get() list(){return this.s.list()} @Roles(Rol.ADMINISTRADOR) @Post() create(@Body() d:CategoriaDto){return this.s.create(d)} @Roles(Rol.ADMINISTRADOR) @Patch(':id') update(@Param('id') id:string,@Body() d:CategoriaUpdateDto){return this.s.update(id,d)}}