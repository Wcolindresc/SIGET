import { ConflictException,Injectable,NotFoundException } from '@nestjs/common'; import { PrismaService } from '../prisma/prisma.service'; import { CategoriaDto,CategoriaUpdateDto } from './dto/categoria.dto'; @Injectable() export class CategoriasService{constructor(private p:PrismaService){} list(){return this.p.categoria.findMany({orderBy:{nombre:'asc'}})} async create(d:CategoriaDto){try{return await this.p.categoria.create({data:d})}catch{throw new ConflictException('Nombre de categoría duplicado')}} async update(id:string,d:CategoriaUpdateDto){const x=await this.p.categoria.findUnique({where:{id}}); if(!x)throw new NotFoundException('Categoría no encontrada'); return this.p.categoria.update({where:{id},data:d})}}