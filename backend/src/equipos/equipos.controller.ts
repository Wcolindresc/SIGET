import { Body,Controller,Get,Param,Patch,Post,Query,UseGuards } from '@nestjs/common'; import { EstadoEquipo,Rol } from '@prisma/client'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { CurrentUser } from '../common/decorators/current-user.decorator'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { EquipoDto,EquipoUpdateDto } from './dto/equipo.dto'; import { EquiposService } from './equipos.service'; @UseGuards(JwtAuthGuard,RolesGuard) @Controller('equipos') export class EquiposController{constructor(private s:EquiposService){} @Get() list(@Query('estado') e?:EstadoEquipo,@Query('categoriaId') c?:string,@Query('buscar') b?:string){return this.s.list(e,c,b)} @Get(':id') one(@Param('id') id:string){return this.s.one(id)} @Roles(Rol.ADMINISTRADOR,Rol.ENCARGADO_INVENTARIO) @Post() create(@Body() d:EquipoDto){return this.s.create(d)} @Roles(Rol.ADMINISTRADOR,Rol.ENCARGADO_INVENTARIO) @Patch(':id') update(@Param('id') id:string,@Body() d:EquipoUpdateDto,@CurrentUser() u:{id:string}){return this.s.update(id,d,u.id)}}