import { Body,Controller,Get,Param,Post,Put,UseGuards } from '@nestjs/common'; import { Rol } from '@prisma/client'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { CurrentUser } from '../common/decorators/current-user.decorator'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { FinalizarMantenimientoDto,MantenimientoDto } from './dto/mantenimiento.dto'; import { MantenimientosService } from './mantenimientos.service'; @UseGuards(JwtAuthGuard,RolesGuard) @Roles(Rol.ADMINISTRADOR,Rol.TECNICO,Rol.ENCARGADO_INVENTARIO) @Controller('mantenimientos') export class MantenimientosController{constructor(private s:MantenimientosService){} @Post() create(@Body() d:MantenimientoDto){return this.s.create(d)} @Get() list(){return this.s.list()} @Get(':id') one(@Param('id') id:string){return this.s.one(id)} @Put(':id/iniciar') iniciar(@Param('id') id:string,@CurrentUser() u:{id:string}){return this.s.iniciar(id,u.id)} @Put(':id/finalizar') finalizar(@Param('id') id:string,@CurrentUser() u:{id:string},@Body() d:FinalizarMantenimientoDto){return this.s.finalizar(id,u.id,d)}}