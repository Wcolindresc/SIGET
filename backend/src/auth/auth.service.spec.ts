import { UnauthorizedException } from '@nestjs/common'; import { AuthService } from './auth.service'; describe('AuthService',()=>{it('rechaza usuario inexistente',async()=>{const prisma:any={usuario:{findUnique:jest.fn().mockResolvedValue(null)}}; const jwt:any={signAsync:jest.fn()}; const s=new AuthService(prisma,jwt); await expect(s.login('nadie@siget.local','12345678')).rejects.toBeInstanceOf(UnauthorizedException);});});