import { Injectable,UnauthorizedException } from '@nestjs/common'; import { JwtService } from '@nestjs/jwt'; import * as bcrypt from 'bcrypt'; import { PrismaService } from '../prisma/prisma.service'; @Injectable() export class AuthService{constructor(private p:PrismaService,private jwt:JwtService){} async login(correo:string,password:string){const u=await this.p.usuario.findUnique({where:{correo}}); if(!u||!u.activo||!(await bcrypt.compare(password,u.passwordHash)))throw new UnauthorizedException('Credenciales inválidas'); return {access_token:await this.jwt.signAsync({sub:u.id,correo:u.correo,rol:u.rol}),usuario:{id:u.id,nombre:u.nombre,apellido:u.apellido,correo:u.correo,rol:u.rol}};}}